import React from "react";

type Props = {
  // Define any props if needed
  guesthouseID?: string;
  guesthouseName?: string;
};

const Guesthousesite = (props: Props) => {
  return <div>Guesthousesite</div>;
};

export default Guesthousesite;
