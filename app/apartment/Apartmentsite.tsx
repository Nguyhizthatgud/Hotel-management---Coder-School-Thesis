import React from "react";

type Props = {
  // Define any props if needed
  apartmentID?: string;
  apartmentName?: string;
};

const Apartmentsite = (props: Props) => {
  return <div>Apartmentsite</div>;
};

export default Apartmentsite;
