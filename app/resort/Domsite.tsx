import React from 'react'

type Props = {
    // Define any props if needed
    domID?: string;
    domName?: string;
}

const Domsite = (props: Props) => {
  return (
    <div>Domsite</div>
  )
}

export default Domsite;